---
interface Props {
  class?: string;
  narrow?: boolean;
}

const { class: className, narrow = false } = Astro.props;
---

<div class:list={['section-container', narrow && 'section-container--narrow', className]}>
  <slot />
</div>
